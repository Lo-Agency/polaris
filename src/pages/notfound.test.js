import React from 'react';
import { render } from '@testing-library/react';

import NotFound from './not-found';

describe('k', () => {
	it('render fuck', () => {
		render(<NotFound />);
	});
});
